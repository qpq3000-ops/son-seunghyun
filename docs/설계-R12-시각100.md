# 설계-R12-시각100 (실물 100% 시각 재현 — 헤더 아이콘줄·보고서 푸터·거래처별채권·판매입력 델타·위젯 아이콘)

> 진실 자료: `docs/참고-실물실측-20260712.md` **§13**(5차 실측, 시각 100% 목표) + **§1**(디자인 토큰).
> 사용자 최우선 지시: **"기능이 들어가든 안 들어가든, 페이지의 글씨체·위치·모양을 실물과 100% 동일하게."**
> R12는 *시각(글씨체·위치·모양)* 재현이지 새 엔진이 아니다. 저장·전잔/후잔·자동분개 로직은 **절대 불변**.

## 0. 대원칙 (R12 불변 규칙)

- **저장·전표·전잔/후잔·자동분개 로직 전부 기존 재사용**. R12는 UI 계층 시각 요소만 추가·보정한다.
- **신규 서버 API = 1건**(거래처별채권 집계, receipts.mjs 확장). **신규 마이그레이션 = 0건.**
- `client/src/styles.css`는 **append-only** — R12-A / R12-B 블록을 파일 끝에 각각 추가. 기존 줄 수정 금지.
  `r8-*`·`r9-*`·`voucher-*`·`vh-*`·`line-toolbar`·`btn small` 등 **기존 클래스 재사용 우선**.
- 기존 메뉴 **id 동결**(삭제·개명 금지). 금액 INTEGER(원) / 수량 REAL(kg). 그리드는 DataGrid(Tabulator).
- 국세청/은행/전자결재/바코드 등 실연동은 **동작 없는 stub**(`disabled` + `title` 안내) 유지.
- **VoucherForm/VoucherScreen 의 저장·전잔/후잔·자동분개 로직 절대 불변** — 슬롯(headerActions/footerActions)·래퍼 마크업으로 시각 요소만 얹는다.
- **App.tsx 수정 시 `groupItems→subgroup` 파생 불변식 유지** — MENUS/`activeGroup`/`subgroups`/`sideItems` 로직은 손대지 않고 `.logobar-right` **안쪽 마크업만** 교체한다(메뉴 사라짐 방지).
- 검증 중 **서버 기동·포트 8010 금지**, **git 커밋 금지**, 코드·문서에 **모델명/AI 식별자 금지**.

## 1. 패키지 분할 (파일 소유 · styles.css 외 겹침 0)

| 패키지 | 파일 | 역할 | 실측 절 |
|---|---|---|---|
| **서버** | `server/receipts.mjs` (확장, 신규 엔드포인트 1) | 거래처별채권 집계 API 1개 | §13.1 |
| **클라A** | `client/src/App.tsx`, **신규** `client/src/components/HeaderIcons.tsx`, `client/src/screens/Dashboard.tsx`, `styles.css`(R12-A) | 상단 헤더 아이콘줄(A) + MyPage 위젯 아이콘(E) | §13.5 / §13.4 |
| **클라B** | `client/src/components/ReportScreen.tsx`, `client/src/screens/StockScreens.tsx`, `client/src/screens/ArByPartnerScreen.tsx`, `client/src/screens/VoucherScreen.tsx`, `client/src/types.ts`, `styles.css`(R12-B) | 보고서 공통 푸터(B) + 거래처별채권 결과(C) + 판매입력 델타(D) | §13.1 / §13.2 |

**겹침 규칙**
- A·B가 함께 건드리는 파일은 `styles.css` **하나뿐**, 서로 다른 append 블록(R12-A / R12-B).
- `types.ts`는 **B만** 편집(A는 import 안 함). `format.ts`는 **아무도 편집 안 함**(타임스탬프 유틸은 ReportScreen.tsx 내부에 두고 B 파일끼리 import → 쓰기 겹침 0).
- **VoucherForm.tsx 는 R12에서 수정하지 않는다**(공용 컴포넌트 보호). 판매입력 델타(D)는 전부 `VoucherScreen.tsx`의 슬롯/래퍼로 처리.
- B의 세 결과 화면(ReportScreen·StockScreens·ArByPartnerScreen)은 `ReportScreen.tsx`가 export 하는 `ReportFooter` 하나를 공유 import 한다(모두 B 소유 → 겹침 아님).

---

## 2. 서버 — 거래처별채권 집계 API (receipts.mjs 확장)

### 2.1 신규 엔드포인트 `GET /api/ar-by-partner?from=YYYY-MM-DD&to=YYYY-MM-DD`
`index.mjs`가 이미 `app.route('/api', receipts)` 로 마운트하므로 **index.mjs 변경 불필요**. `receipts.mjs`에 라우트 하나만 추가.

집계 정의(§13.1, 지시서 그대로):
- **기초채권**(`opening`) = 기간 시작(`from`) **직전**까지의 잔액 = Σ(sale total, `io_date < from`) − Σ(수금, `io_date < from`)
- **재고매출**(`stock_sales`) = 기간 내 sale 합 = Σ(sale total, `from ≤ io_date ≤ to`)
- **회계매출**(`acct_sales`) = `0` (회계전표 기반 매출 — 현 단계 미산출)
- **수금합계**(`receipt_total`) = 기간 내 수금 합 = Σ(receipt.amount, kind='수금', `from ≤ io_date ≤ to`)
- **기타할인등차액**(`etc_diff`) = `0`
- **잔액**(`balance`) = `opening + stock_sales + acct_sales − receipt_total − etc_diff`
- 활동 없는 거래처(세 축 전부 0)는 제외 → **데이터 없으면 `[]`**(빈 그리드). 정렬: 거래처명 오름차순.

> `total_amount`(공급가+부가세)를 채권 매출로 사용 — 기존 `/receivables` 와 동일 기준(채권 잔액 = 매출총액 − 수금).

### 2.2 구현 (receipts.mjs 하단에 append)
```js
// ── 거래처별채권 (기간 집계) ─────────────────────────────── (설계-R12-시각100 §2)
// 기초채권 = 기간前 잔액, 재고매출 = 기간내 sale 합, 수금합계 = 기간내 수금 합,
// 회계매출·기타할인등차액 = 0, 잔액 = 기초+매출-수금. 활동없는 거래처 제외 → 없으면 [].
receipts.get('/ar-by-partner', (c) => {
  const from = isValidDate(c.req.query('from')) ? c.req.query('from') : todayISO();
  const to   = isValidDate(c.req.query('to'))   ? c.req.query('to')   : todayISO();
  const rows = db.prepare(`
    SELECT p.id AS partner_id, p.code AS partner_code, p.name AS partner_name,
      COALESCE((SELECT SUM(d.total_amount) FROM doc d
                WHERE d.doc_type='sale' AND d.partner_id=p.id AND d.io_date < ?), 0)
      - COALESCE((SELECT SUM(r.amount) FROM receipt r
                WHERE r.kind='수금' AND r.partner_id=p.id AND r.io_date < ?), 0) AS opening,
      COALESCE((SELECT SUM(d.total_amount) FROM doc d
                WHERE d.doc_type='sale' AND d.partner_id=p.id AND d.io_date >= ? AND d.io_date <= ?), 0) AS stock_sales,
      COALESCE((SELECT SUM(r.amount) FROM receipt r
                WHERE r.kind='수금' AND r.partner_id=p.id AND r.io_date >= ? AND r.io_date <= ?), 0) AS receipt_total
    FROM partner p
  `).all(from, from, from, to, from, to);
  return c.json(
    rows
      .map(r => ({ ...r, acct_sales: 0, etc_diff: 0,
                   balance: r.opening + r.stock_sales - r.receipt_total }))
      .filter(r => r.opening !== 0 || r.stock_sales !== 0 || r.receipt_total !== 0)
      .sort((a, b) => a.partner_name.localeCompare(b.partner_name, 'ko'))
  );
});
```
`err`/`isValidDate`/`todayISO`/`db`는 파일 상단에서 이미 import 됨(추가 import 없음). 기존 `/receipts`·`/receivables`·`/payables` 라우트는 **무변경**.

---

## 3. 클라A — 상단 헤더 아이콘줄(A) + MyPage 위젯 아이콘(E)

### 3.1 신규 `client/src/components/HeaderIcons.tsx` (§13.5)
실물 순서·크기(~28px)·간격(~34px) 재현. 16개는 이모지, 17번째(앱그리드)만 인라인 SVG, 18번째는 프로필 원형(회색 테두리). 전부 **기능 무관 시각 스텁**(title 안내). 연결 가능한 기존 쪽지/알림 화면이 아직 없으므로 onClick 없음(추후 화면 생기면 title→onClick 대체). 프로필만 `onProfile` 콜백으로 환경설정 진입(기존 ⚙️ 단축 기능 보존).

```tsx
// 상단 헤더 아이콘줄 (참고-실물실측 §13.5) — 실물 순서·크기·간격 시각 재현.
// 전부 기능 무관 스텁(title 안내). 프로필 원형만 onProfile(환경설정) 연결.
const ICONS: { g: string; t: string }[] = [
  { g: '❄️', t: '즐겨찾기 정리 (기능 예정)' },
  { g: '🌙', t: '화면 모드 (기능 예정)' },
  { g: '🔍', t: '검색 (기능 예정)' },
  { g: '🎧', t: '고객센터 (기능 예정)' },
  { g: '📄', t: '새 문서 (기능 예정)' },
  { g: '📝', t: '메모 (기능 예정)' },
  { g: '🔔', t: '알림 (기능 예정)' },
  { g: '🎀', t: '이벤트 (기능 예정)' },
  { g: '💬', t: '쪽지 (기능 예정)' },
  { g: '📱', t: '문자(SMS) (기능 예정)' },
  { g: '✉️', t: '메일 (기능 예정)' },
  { g: '🔒', t: '보안 (기능 예정)' },
  { g: '🖥️', t: '원격지원 (기능 예정)' },
  { g: '🕐', t: '최근 실행 (기능 예정)' },
  { g: '📒', t: '장부 (기능 예정)' },
  { g: '💰', t: '자금 (기능 예정)' },
];

// 17번: 컬러 도트 그리드(전체 아이콘/앱런처) — 이모지로 대체 불가 → 인라인 SVG
function AppGrid() {
  const cols = ['#e5484d', '#f5a623', '#30a46c', '#1f9d55', '#3b82f6', '#8b5cf6',
                '#ec4899', '#06b6d4', '#f59e0b', '#10b981', '#6366f1', '#ef4444'];
  return (
    <svg viewBox="0 0 20 20" aria-hidden="true">
      {cols.map((c, i) => (
        <rect key={i} x={2 + (i % 3) * 6} y={1 + Math.floor(i / 3) * 5}
              width={4} height={4} rx={1} fill={c} />
      ))}
    </svg>
  );
}

export function HeaderIcons({ onProfile }: { onProfile?: () => void }) {
  return (
    <div className="hdr-icons">
      {ICONS.map((it, i) => (
        <button key={i} type="button" className="hdr-icon" title={it.t}>{it.g}</button>
      ))}
      <button type="button" className="hdr-icon" title="전체 아이콘 (기능 예정)"><AppGrid /></button>
      <button type="button" className="hdr-profile" title="내 정보 / 환경설정" onClick={onProfile}>👤</button>
    </div>
  );
}
```
> 앱그리드 SVG는 3열×4행 12개 멀티컬러 도트로 §13.5의 "컬러 도트 그리드(3×4 멀티컬러)"를 근사. 색/크기는 자유 조정 가능(외부 리소스 없음 — CSP 안전).

### 3.2 App.tsx 수정 (`.logobar-right` 안쪽만 교체)
`import { HeaderIcons } from './components/HeaderIcons';` 추가. `.logobar-right` 블록을 아래로 교체(**그 외 App.tsx 로직·MENUS·subgroup 파생 전부 불변**):
```tsx
// 기존
<div className="logobar-right">
  <button className="icon-btn" title="환경설정" onClick={() => open('settings')}>⚙️</button>
  <div className="profile-dot" title="단일 사용자">☺</div>
</div>
// 교체
<div className="logobar-right">
  <HeaderIcons onProfile={() => open('settings')} />
</div>
```
- 실물엔 헤더 ⚙️가 없음 → 제거하되 환경설정 진입은 **프로필 원형 클릭**으로 보존(+ Self-Customizing 메뉴/메뉴검색으로도 여전히 도달 가능). `.profile-dot` CSS는 미사용으로 남겨둠(무해).
- **즐겨찾기 바 최우측 고정핀 📌**(`.favbar-pin`, App.tsx line 103)은 **이미 구현됨 → 유지**(추가 작업 없음).

### 3.3 Dashboard.tsx — MyPage 위젯 헤더 아이콘 (§13.4 / E)
`icons` 상수를 4개(↗ ⟳ ✎ ⋮)로 교체. 실물 스타일(회색, hover 진해짐). 각 아이콘 개별 `title` 스텁:
```tsx
// 기존
const icons = (
  <span className="widget-icons" title="위젯 도구 (Phase 4)">
    <span>⟳</span><span>⋯</span>
  </span>
);
// 교체
const icons = (
  <span className="widget-icons">
    <span title="새 창으로 열기 (기능 예정)">↗</span>
    <span title="새로고침 (기능 예정)">⟳</span>
    <span title="편집 (기능 예정)">✎</span>
    <span title="위젯 옵션 (기능 예정)">⋮</span>
  </span>
);
```
> `icons` 는 6개 위젯 head 에 공유되므로 한 곳만 고치면 전 위젯 반영. 위젯 배치(mp6 3열)·데이터·"등록된 데이터가 없습니다" 문구 전부 불변.

### 3.4 styles.css — R12-A 블록(파일 끝 append)
```css
/* ══ R12-A 시각100 — 상단 헤더 아이콘줄 + MyPage 위젯 아이콘 (append-only) ══ */

/* §13.5 상단 아이콘줄 — 로고바 우측, ~28px 히트영역 + 6px gap ≒ 34px 피치 */
.hdr-icons { display: flex; align-items: center; gap: 6px; }
.hdr-icon {
  width: 28px; height: 28px; padding: 0; border: 0; background: none; cursor: default;
  display: inline-flex; align-items: center; justify-content: center;
  font-size: 17px; line-height: 1; border-radius: 6px; color: #4b5563; font-family: inherit;
}
.hdr-icon:hover { background: #f0f2f5; }
.hdr-icon svg { width: 18px; height: 18px; display: block; }
/* 최우측 프로필 원형(회색 테두리) */
.hdr-profile {
  width: 28px; height: 28px; margin-left: 4px; padding: 0; cursor: pointer;
  border: 1.5px solid #c4cad3; border-radius: 50%; background: #eef0f3; color: #7a8291;
  display: inline-flex; align-items: center; justify-content: center; font-size: 15px;
}
.hdr-profile:hover { border-color: #9aa4b1; }

/* §13.4 MyPage 위젯 헤더 아이콘 ↗ ⟳ ✎ ⋮ — 회색 유지 + hover 진해짐 */
.widgets.mp6 .widget-icons { gap: 7px; }
.widgets.mp6 .widget-icons span { cursor: default; transition: color .1s; }
.widgets.mp6 .widget-icons span:hover { color: #495057; }
```
> `.widget-icons` 기본색(#b6bdc9 회색)은 기존 line 419 규칙 그대로 상속 → hover만 R12-A로 진하게. 헤더 아이콘 간격 34px는 `28 + 6`으로 맞춤(실측 "약 28px 크기·34px 간격").

---

## 4. 클라B — 보고서 푸터(B) + 거래처별채권 결과(C) + 판매입력 델타(D)

### 4.1 보고서 공통 푸터 (§13.1 / B) — ReportScreen 확장 + 공유 컴포넌트
결과형 화면 하단 공통 푸터: **좌 `[P:1]` + 우 조회 타임스탬프 `YYYY/MM/DD 오후 h:mm:ss`**.

**(1) ReportScreen.tsx — `ReportFooter` export + 타임스탬프 유틸**(파일 내 추가):
```tsx
// 조회시각 표기 — "2026/07/12 오후 11:12:49"(참고 §13.1). 로컬 24→12시제 + 오전/오후.
function fmtQueryStamp(d: Date): string {
  const p = (n: number) => String(n).padStart(2, '0');
  let h = d.getHours();
  const ampm = h < 12 ? '오전' : '오후';
  h = h % 12 || 12;
  return `${d.getFullYear()}/${p(d.getMonth() + 1)}/${p(d.getDate())} ${ampm} ${h}:${p(d.getMinutes())}:${p(d.getSeconds())}`;
}

// 결과형 화면 공통 푸터(좌 [P:n] + 우 조회시각). 재고현황·거래처별채권·ReportScreen real 이 공유.
export function ReportFooter({ page = 1, at }: { page?: number; at?: Date }) {
  return (
    <div className="r12-report-footer">
      <span className="r12-pagemark">[P:{page}]</span>
      <span className="r12-querystamp">{fmtQueryStamp(at ?? new Date())}</span>
    </div>
  );
}
```

**(2) `ReportRealOpt` 인터페이스에 옵션 추가**(기본 on):
```ts
export interface ReportRealOpt {
  centerTitle?: string;
  companyLine?: boolean;
  asOfLine?: boolean;
  negativeField?: string;
  bottomButtons?: { label: string; primary?: boolean; split?: boolean; stub?: string }[];
  footer?: boolean;        // 기본 true — [P:1]+조회시각 공통 푸터. false면 미표기
}
```

**(3) 조회시각 상태 + 렌더**: `ReportScreen` 함수에 `const [queriedAt, setQueriedAt] = useState<Date>(() => new Date());` 추가하고, `runSearch` 성공(`setEnv(data)`) 직후 `setQueriedAt(new Date());`. 그리드(`.screen-grid`) 아래·`real?.bottomButtons` 위에 삽입:
```tsx
{real && real.footer !== false && <ReportFooter at={queriedAt} />}
```
> reportDefs 의 `real` 을 쓰는 화면(현재 `stock-flow`)은 자동으로 푸터를 얻는다. 별도 def 변경 불필요.

### 4.2 재고현황(StockScreens `StockStatus`) 푸터 적용 (§13.1)
`import { ReportFooter } from '../components/ReportScreen';` 추가. `const [queriedAt, setQueriedAt] = useState<Date>(() => new Date());` 추가, `load()` 성공(`setRows(...)`) 직후 `setQueriedAt(new Date());`. `.r8-real` 블록에서 `.screen-grid` 아래·`.r8-report-bottom` 위에:
```tsx
<ReportFooter at={queriedAt} />
```
그 외 StockStatus(검색폼·타이틀바·3열 그리드·인쇄/Excel/자동알림) 전부 불변.

### 4.3 거래처별채권(ArByPartnerScreen) 결과 실물화 (§13.1 / C)
검색폼은 **현행 유지**. 결과 영역을 실물 프레임으로 교체 + 신규 API 연결.

**(1) 데이터/타입**: `types.ts`에 신규 인터페이스 추가(B 소유):
```ts
// 거래처별채권 1행 (GET /api/ar-by-partner) — 설계-R12-시각100 §2
export interface ArByPartnerRow {
  partner_id: number; partner_code: string; partner_name: string;
  opening: number;        // 기초채권
  stock_sales: number;    // 재고매출
  acct_sales: number;     // 회계매출(=0)
  receipt_total: number;  // 수금합계
  etc_diff: number;       // 기타할인등차액(=0)
  balance: number;        // 잔액
}
```

**(2) ArByPartnerScreen.tsx 변경점**:
- import 교체·추가: `import type { ArByPartnerRow } from '../types';` + `import { ReportFooter } from '../components/ReportScreen';` + `import { fmtWon, periodPreset, todayISO } from '../format';` (기존과 동일).
- 상태: `rows` 타입을 `ArByPartnerRow[]` 로, `companyName`·`queriedAt` 추가.
- 마운트 시 회사명 1회 조회(재고현황과 동일 패턴):
  ```tsx
  const [companyName, setCompanyName] = useState('');
  useEffect(() => {
    api.get<Record<string, string>>('/api/settings')
      .then(s => setCompanyName(s.company_name ?? '')).catch(() => setCompanyName(''));
  }, []);
  ```
- `load` 를 신규 엔드포인트로 교체(기간 from/to 사용, 거래처 선택 시 클라 필터 유지):
  ```tsx
  const load = useCallback(async (f = from, t = to) => {
    try {
      const all = await api.get<ArByPartnerRow[]>(`/api/ar-by-partner?from=${f}&to=${t}`);
      setRows(partnerId ? all.filter(r => r.partner_id === partnerId) : all);
      setQueriedAt(new Date());
    } catch (e) { toast.show((e as Error).message, 'error'); }
  }, [from, to, partnerId, toast]);
  ```
  (프리셋 버튼 `preset`/`presetMonthPlus`/`reset` 은 `load(f, t)` 형태로 from·to 둘 다 넘기도록 소폭 조정.)
- **결과 프레임 교체** — 기존 `<div className="screen-grid r8-real">…</div>` 를 아래로 교체:
  ```tsx
  <div className="r8-real">
    <div className="r8-report-title">거래처별채권</div>
    <div className="r8-report-meta">
      <span>회사명 : {companyName}</span>
      <span>{from.split('-').join('/')} ~ {to.split('-').join('/')}</span>
    </div>
    <div className="screen-grid">
      <DataGrid<ArByPartnerRow> columns={columns} data={rows}
        gridRef={t => { gridRef.current = t; }} />
    </div>
    <ReportFooter at={queriedAt} />
    <div className="r8-report-bottom">
      <button className="btn r8-primary r8-split" onClick={() => window.print()}>인쇄</button>
      <button className="btn r8-split-caret">▲</button>
      <button className="btn r8-ghost"
        onClick={() => gridRef.current?.download('xlsx', '거래처별채권.xlsx', { sheetName: '거래처별채권' })}>Excel</button>
      <button className="btn r8-ghost" disabled title="자동알림은 연동 예정입니다">자동알림</button>
    </div>
  </div>
  ```
- **7컬럼 정의**(전부 ▼정렬 = R12-B CSS가 헤더에 ▼ 상시표기; 숫자 우측정렬·콤마; 음수 그대로 검정). 하단 소계행은 Tabulator `bottomCalc` 로 굵은 '합계' 행:
  ```tsx
  const won = (c: CellComponent) => fmtWon(Number(c.getValue() ?? 0));
  const wonBold = (c: CellComponent) => `<b>${fmtWon(Number(c.getValue() ?? 0))}</b>`;
  const columns: ColumnDefinition[] = [
    { title: '거래처명', field: 'partner_name', minWidth: 160, hozAlign: 'left',
      bottomCalc: () => '합계', bottomCalcFormatter: c => `<b>${c.getValue() ?? ''}</b>` },
    { title: '기초채권', field: 'opening',       width: 120, hozAlign: 'right', formatter: won, bottomCalc: 'sum', bottomCalcFormatter: wonBold },
    { title: '재고매출', field: 'stock_sales',   width: 120, hozAlign: 'right', formatter: won, bottomCalc: 'sum', bottomCalcFormatter: wonBold },
    { title: '회계매출', field: 'acct_sales',    width: 120, hozAlign: 'right', formatter: won, bottomCalc: 'sum', bottomCalcFormatter: wonBold },
    { title: '수금합계', field: 'receipt_total', width: 120, hozAlign: 'right', formatter: won, bottomCalc: 'sum', bottomCalcFormatter: wonBold },
    { title: '기타할인등차액', field: 'etc_diff', width: 130, hozAlign: 'right', formatter: won, bottomCalc: 'sum', bottomCalcFormatter: wonBold },
    { title: '잔액', field: 'balance', width: 130, hozAlign: 'right', formatter: c => `<b>${fmtWon(Number(c.getValue() ?? 0))}</b>`, bottomCalc: 'sum', bottomCalcFormatter: wonBold },
  ];
  ```
  (`CellComponent` 타입은 `tabulator-tables` 에서 import. 기존 파일이 `ColumnDefinition` 만 import 중이면 타입만 추가.)
- **데이터 없으면**: 신규 API가 `[]` 반환 → DataGrid placeholder("자료가 없습니다")로 빈 그리드. 소계행은 0 표기.
- **주의(§13.1 '/ 계' 소계)**: 대표거래처 그룹 소계('/ 계')는 그룹핑 기능(대표거래처 합산 stub)이 있어야 나오므로 R12 범위 밖 → **총 '합계' 소계행만** 굵게 렌더(굵은 소계행 요건 충족). 담당자별·거래처관리담당자·잔액범위·그래프 등은 기존대로 stub 유지.
- **음수 검정**: 기존 `미수잔액` 컬럼의 빨강 강조(`danger-text`)를 제거하고 위 정의처럼 검정 굵게로 교체(참고 §13.1 "음수 그대로 검정").

### 4.4 판매입력 라이브 델타 (§13.2 / D) — VoucherScreen 슬롯만
**VoucherForm.tsx 무변경.** 아래 전부 `VoucherScreen.tsx` 에서 처리. 저장·전잔/후잔·자동분개 로직 **불변**.

**(1) 조건 저장탭 — 첫 탭 파랑 pill (§13.2)**: 판매입력 신규 모드에서만 VoucherForm 위에 저장탭 스트립을 얹는다. 회사명은 기존 settings 조회 effect에 얹어 획득(저장 로직 무관):
```tsx
const [companyTab, setCompanyTab] = useState('');
useEffect(() => {
  api.get<Record<string, string>>('/api/settings')
    .then(s => { setVatRound(s.vat_round === 'round' ? 'round' : 'floor'); setCompanyTab(s.company_name ?? ''); })
    .catch(() => {});
}, []);
```
`return (<div className="screen"> …` 의 최상단(‹VoucherForm› 바로 위)에 삽입:
```tsx
{kind === 'sale' && mode === 'create' && (
  <div className="r12-savetabs">
    <button className="r12-savetab on" title="저장된 검색조건 탭">{companyTab || '기본'} ▼</button>
    <button className="r12-savetab-add" disabled title="조건 저장 탭 추가는 연동 예정입니다">+</button>
  </div>
)}
```
> 실물의 첫 탭 `[블랙크랙원 ▼]`(파랑 pill) 재현. 기존 `.cond-tab.on`(흰 pill·accent 글자)과 달라서 신규 `.r12-savetab.on`(파랑 채움)으로 **보정**. 판매입력엔 실제 조건저장 기능이 없으므로 시각 스텁.

**(2) 그리드 툴바 16종 (§13.2)** — sale 의 `headerActions` 를 아래 순서로 교체(기존 8개 동작 유지 + 신규 8개 disabled 스텁). 가로 넘침 대비 기존 `.r11a-toolbar`(overflow-x:auto) 래퍼로 감싼다:
```tsx
headerActions={
  kind === 'sale' ? (
    <div className="r11a-toolbar">
      <button className="btn small" onClick={() => setFindOpen(true)}>찾기(F3)</button>
      <button className="btn small" onClick={sortLines}>정렬</button>
      <button className="btn small" onClick={copyRecent}>거래내역보기(판매)▼</button>
      <button className="btn small" disabled title="연동 예정입니다">My품목▼</button>
      <button className="btn small" disabled title="연동 예정입니다">소요</button>
      <button className="btn small" disabled title="연동 예정입니다">주문</button>
      <button className="btn small" disabled title="연동 예정입니다">구매</button>
      <button className="btn small" disabled title="연동 예정입니다">보류</button>
      <button className="btn small" disabled title="연동 예정입니다">할인</button>
      <button className="btn small" onClick={() => setStockPickOpen(true)}>재고불러오기</button>
      <button className="btn small" disabled title="연동 예정입니다">바코드</button>
      <button className="btn small" disabled title="연동 예정입니다">전표바코드</button>
      <button className="btn small" onClick={verify}>검증</button>
      <button className="btn small" onClick={() => setProfitOpen(true)}>이익계산</button>
      <button className="btn small" onClick={() => setPullOpen(true)}>전표불러오기</button>
      <button className="btn small" onClick={vatSummary}>거래별부가세계산</button>
    </div>
  ) : (
    <button className="btn small" onClick={() => setPullOpen(true)}>발주서 불러오기</button>
  )
}
```
> 순서는 §13.2 실측 그대로: 찾기·정렬·거래내역보기(판매)▼·My품목▼·소요·주문·구매·보류·할인·재고불러오기·바코드·전표바코드·검증·이익계산·전표불러오기·거래별부가세계산. `거래내역보기(판매)▼`·`My품목▼` 는 ▼(스플릿 표시)만 라벨에 부착. 구매(purchase)입력 headerActions 는 무변경.

**(3) 하단 버튼 — 회계전표연결·현금수금▲ 추가 (§13.2)**: 신규 모드 sale 에 `footerActions` 를 부여(현재 create 모드는 저장(F8)만 노출). 수정(edit) 모드는 기존 유지:
```tsx
footerActions={
  mode === 'edit' ? (
    <>
      <button className="btn" onClick={onCancel}>목록으로</button>
      {kind === 'sale' && <button className="btn" onClick={doPrint}>인쇄</button>}
      <button className="btn danger" onClick={() => setConfirmDel(true)}>삭제</button>
    </>
  ) : kind === 'sale' ? (
    <>
      <button className="btn small" disabled title="연동 예정입니다">저장/전표(F7)</button>
      <button className="btn small" disabled title="회계전표연결은 연동 예정입니다">회계전표연결</button>
      <button className="btn small" onClick={resetLines}>다시작성</button>
      <button className="btn small" disabled title="현금수금은 연동 예정입니다">현금수금▲</button>
      <button className="btn small" disabled title="연동 예정입니다">리스트</button>
      <button className="btn small" disabled title="연동 예정입니다">웹자료올리기</button>
    </>
  ) : undefined
}
```
`resetLines` 는 **UI 전용**(저장·전잔/후잔·분개 미개입) 라인 초기화:
```tsx
const resetLines = () => { setLines([emptyLine(), emptyLine(), emptyLine()]); setSourceDocId(null); };
```
> VoucherForm 이 렌더하는 기본 `저장(F8)`(파랑) + ✈ 최좌측·스플릿▲ 미세 배치는 공용 컴포넌트 보호를 위해 R11과 동일한 **의도적 미세 편차**로 남긴다. 나머지 하단바는 footerActions 로 채움. **`전잔·후잔` 필드(prevBalance)·저장(save)·자동분개는 일절 손대지 않음.**

### 4.5 styles.css — R12-B 블록(파일 끝 append)
```css
/* ══ R12-B 시각100 — 보고서 공통 푸터 + 거래처별채권 결과 + 판매입력 저장탭 (append-only) ══ */

/* §13.1 보고서 공통 푸터 — 좌 [P:1] + 우 조회시각 */
.r12-report-footer { display: flex; align-items: center; justify-content: space-between;
  font-size: 12px; color: #495057; padding: 6px 2px 2px; }
.r12-pagemark { color: #495057; }
.r12-querystamp { color: #6b7280; font-variant-numeric: tabular-nums; }

/* §13.1 결과 그리드 머리글 정렬화살표 ▼ 상시 표기(모든 r8-real 그리드 공통) */
.r8-real .tabulator .tabulator-col .tabulator-col-sorter { display: none; }
.r8-real .tabulator .tabulator-col.tabulator-sortable .tabulator-col-title { padding-right: 12px; }
.r8-real .tabulator .tabulator-col.tabulator-sortable .tabulator-col-title::after {
  content: '▼'; font-size: 9px; color: #b6bdc9; margin-left: 3px; }

/* §13.2 판매입력 조건 저장탭 — 첫 탭 파랑 pill(사용자 저장탭) + (+) */
.r12-savetabs { display: flex; align-items: center; gap: 4px; margin: 0 0 8px; }
.r12-savetab { border: 0.667px solid var(--r8-border); background: #fff; color: #495057;
  border-radius: 13px; padding: 4px 12px; font-size: 12px; cursor: default; font-family: inherit; }
.r12-savetab.on { background: var(--r8-primary); border-color: var(--r8-primary); color: #fff; font-weight: 700; }
.r12-savetab-add { border: 0.667px solid var(--r8-border); background: #fff; color: var(--sub);
  border-radius: 13px; padding: 4px 11px; font-size: 12px; cursor: not-allowed; font-family: inherit; }
```
그 외 거래처별채권 결과(중앙제목·회사/기간 줄·하단바)는 기존 `r8-report-title`/`r8-report-meta`/`r8-report-bottom`/`r8-primary`/`r8-split`/`r8-ghost` 재사용(신규 CSS 없음). 판매입력 툴바는 `r11a-toolbar`(R11-A) 재사용.

---

## 5. 검증 계획 (포트/기동 금지)

- **타입/빌드**: `npm run build`(client) 또는 `tsc --noEmit` 로 A·B 컴파일 확인. 서버는 `node --check server/receipts.mjs`.
- **서버 로직 단위검증**(DB 쓰기 없음): `/api/ar-by-partner` 는 순수 조회(SELECT) → `BEGIN … ROLLBACK` 불필요. 기존 seed DB로 `from/to` 경계값(기초=경계前, 재고매출/수금=경계内)·빈결과([]) 케이스만 논리 확인.
- **회귀(불변 확인)**:
  - 판매입력 저장(`save`)·`전잔/후잔`(prevBalance) 계산·자동분개 차=대·doc_no 채번이 R12 후에도 동일한지(툴바/하단바/저장탭은 시각 요소라 로직 미개입).
  - App.tsx 대메뉴·서브탭·사이드바(`groupItems→subgroups→sideItems`)가 그대로 렌더되는지(메뉴 누락 없음), 즐겨찾기 📌·⭐ 동작 불변.
  - ReportScreen `real` 보고서(재고변동표)·재고현황·거래처별채권 하단에 `[P:1]`+조회시각 푸터가 붙고, 기존 인쇄/Excel/자동알림 버튼 동작 불변.
- **시각 확인**(선택): 헤더 아이콘 17+프로필 정렬·크기(28/34px), 위젯 4아이콘 hover, 판매입력 첫 저장탭 파랑 pill, 거래처별채권 7열·굵은 합계행·음수 검정.

## 6. 산출물 체크리스트

**서버**: `receipts.mjs` — `GET /api/ar-by-partner` 1건 추가(기초/재고매출/회계매출0/수금/기타0/잔액), index.mjs·마이그레이션 변경 없음.
**클라A**: `App.tsx`(`.logobar-right` → `<HeaderIcons onProfile={()=>open('settings')}/>`, import 추가), **신규** `components/HeaderIcons.tsx`(16이모지+앱그리드 SVG+프로필), `screens/Dashboard.tsx`(위젯 `icons` → ↗⟳✎⋮), `styles.css` R12-A 블록.
**클라B**: `components/ReportScreen.tsx`(`ReportFooter` export+`fmtQueryStamp`+`ReportRealOpt.footer`+queriedAt 렌더), `screens/StockScreens.tsx`(재고현황 ReportFooter), `screens/ArByPartnerScreen.tsx`(결과 실물 프레임+신규 API+7열), `screens/VoucherScreen.tsx`(저장탭·툴바16·하단버튼·resetLines·companyTab), `types.ts`(`ArByPartnerRow`), `styles.css` R12-B 블록.
**불변**: `VoucherForm.tsx`(무변경), MENUS/subgroup 파생, 기존 CSS 줄, 금액/수량 단위, git 커밋.
