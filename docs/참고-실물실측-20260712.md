# 이카운트 실물 실측 참고서 (2026-07-12, 라이브 접속)

> 지휘관(총괄 세션)이 실제 이카운트(블랙크랙원 로스터스, loginba.ecount.com)에 로그인해
> F12 수준으로 직접 측정·캡처한 값. **이 문서의 수치가 기존 추정치보다 우선한다.**
> 목표: 화면 UI/UX/기능 80% 이상 실물 일치. 국세청/은행 실연동은 계속 '연동 예정' stub.

## 1. 디자인 토큰 (getComputedStyle 실측)

| 항목 | 실측값 |
|---|---|
| 폰트 스택 | `'Malgun Gothic', Arial, 'Apple SD Gothic Neo', Gulim` |
| body | 12px / 400 / #000 / 배경 `#F7F8F9` |
| 즐겨찾기 바(최상단) | 배경 `#E9ECEF`, 높이 26px, 링크 12px `#495057` |
| 대메뉴 탭(MyPage 등) | 12px, `#142444`, padding 2px 12px, radius 5px. 활성 탭: 연파랑 pill 배경+파랑 글자 |
| 위젯 카드 | 배경 #FFF, **radius 10px**, border 0.667px solid `#DEE2E6`, 그림자 없음 |
| 위젯 제목 | 15px / 700 / #000 |
| 그리드 머리글(th) | 12px / 700 / #000, 배경 `#F7F8F9`, 가운데 정렬, padding 7px 3px 6px, 높이 31px, 정렬 화살표 ▼ |
| 코드/일자 링크 | `#19358C`, 밑줄 없음, 12px |
| **마이너스 수량 셀** | 배경 `#F2DEDE` (보고서 그리드에서만. MyPage 위젯에서는 검정 일반 표기) |
| 보고서 중앙 제목 | 24px / 700 / 가운데 (예: "재고현황") |
| 주(파랑) 버튼: 인쇄·검색(F8)·Search(F3)·신규(F2) | 배경 `#1B3DA1`, 흰 글자, 12px, padding 7px 10px, radius 5px. 스플릿 버튼은 좌측만 라운드(5px 0 0 5px)+▲ 드롭다운 |
| 보조 버튼(Excel·Option·도움말 등) | 흰 배경, 글자 `#142444`, border 0.667px `#DEE2E6`, radius 5px |
| 숫자 셀 | 우측 정렬, 콤마 표기 |

## 2. MyPage 대시보드 (실물 배치 — 최우선 구현)

헤더: `< 2026/07 > 일정관리` (연/월 네비) + 우측 아이콘(↗ ✎ ⋮).
**3열 배치** (폭 비율 약 34% : 20% : 41%):

| 열1 | 열2 | 열3(넓음) |
|---|---|---|
| 재고현황 | To Do | 판매현황 |
| 쪽지함리스트 | (세금)계산서진행단계 → 열2~3 가로 스팬 | 매입매출장 |

- **재고현황 위젯**: 품목코드(파랑 링크) / 품목명[규격] / 재고수량(우측정렬, 음수도 검정). 규격 없으면 `[kg]`처럼 단위 표기가 붙음. 행 줄무늬(짝수행 연회색 #F7F8F9).
- **판매현황 위젯**: **전표의 품목 줄 단위**로 행 생성(같은 "2026/07/02 -1"이 품목별로 반복). 컬럼: 일자-No.(파랑 링크) / 품목명[규격] / 수량 / **단가** / 공급가액 / 부가세 / 합계 / 거래처명. 단가 = 공급가액÷수량 반올림 표기(실물: 3,230,000/114=28,364).
- **To Do**: 없으면 "등록된 데이터가 없습니다." (실물 문구)
- **쪽지함리스트 / 매입매출장 / (세금)계산서진행단계**: 이 회사 데이터 없음 → 제목 + "등록된 데이터가 없습니다." 형태. 쪽지함은 그룹웨어 쪽지(수신 최근 N건), 매입매출장은 회계Ⅰ 매입매출장 최근 행, 계산서진행단계는 세무 위젯.
- 클론의 기존 '미수금 TOP'·'달력 미니' 위젯은 **기본 화면에서 제외**(코드는 보존 — 추후 위젯 설정 기능용).

## 3. 재고현황 (재고Ⅰ>출력물, E040701)

검색폼은 기존 R7-b 매칭 유지. **결과 화면을 실물화**:
- 타이틀바: ★ 재고현황 (좌) / 우측: [입력 후 [Enter]] 인풋 + `Search(F3)` 파랑버튼 + Option + 도움말
- 본문 상단: 가운데 24px 굵은 제목 "재고현황", 그 아래 좌 "회사명 : 블랙크랙원 로스터스"(환경설정 상호) / 우 기준일 "2026/07/12"
- 그리드 3열: 품목코드▼(가운데 머리글, 셀은 파랑 링크) / 품목명[규격]▼(좌) / 재고수량▼(우). **음수 재고 셀 배경 #F2DEDE**
- 하단 버튼: `인쇄`(파랑 스플릿+▲) / `Excel` / `자동알림`

## 4. 판매조회 (재고Ⅰ>영업관리, E040206)

- 타이틀바: ★ 판매조회 + 상태 pill 필터 [**전체**(파랑 채움)/결재중/미확인/확인] + [입력 후 Enter] + `Fn` + `Search(F3)` + Option + 도움말
- 그 아래줄: 페이지네이션 `① 2 » [입력]/2` (좌) + 조회기간 표기 "2026/06/01 ~ 2026/07/12" (우)
- 그리드 컬럼: [체크박스] / 일자-No.(파랑 링크, "2026/07/09 -3" 형식) / 거래처명 / 품목명(요약)("bco 블랜딩 [kg] 외 1건") / 금액합계(우측) / 거래유형명("부가세율 적용") / 창고명 / 회계반영여부(초록 ✓) / 인쇄(링크·인쇄완료분은 주황 배지)
- 순번: 회색 테두리 사각 배지(1,2,3…). 행 높이 큼(셀 멀티라인 허용), 모든 셀에 옅은 보더.

## 5. 거래처리스트 (회계Ⅰ>기초등록)

- 타이틀바: ★ 거래처리스트 + [사용중단포함] 토글 + [입력 후 Enter] + Fn + Search(F3) + Option + 도움말
- 그리드: [체크박스] / 거래처코드▼ / 거래처명▼(파랑 링크) / 대표자명▼ / 전화▼ / 모바일▼ / 검색창내용▼ / 사용구분(YES) / 이체정보("등록" 파랑 링크)
- 하단 버튼: `신규(F2)`(파랑 스플릿▲) / 관계설정 / 계층그룹 / 변경 / 사용중단/재사용▲ / Excel▲ / 웹자료올리기 / SMS
- 참고: 실물에서 거래처등록은 **회계Ⅰ>기초등록** 소속 (재고Ⅰ 아님). 메뉴 id 동결 규칙 내에서 조정 방법은 설계자가 결정(기존 id 삭제 금지·추가는 허용).

## 6. 거래명세서인쇄 (재고Ⅰ>영업관리, E040210) — 기존 R7-c 매칭 유지 + 보강

- 검색조건 탭: `기본` `전체` `+` (전체 탭 실물 존재 확인)
- 기준일자: [금월(~오늘)] 파란 배지 + 년/월/일 셀렉트 ×2(범위) + 달력 아이콘
- 내.외자구분 라디오(●전체/내자/외자), 창고/거래처/품목/담당자/거래처관리담당자 검색인풋, 기타 체크박스 2개
- 하단 고정바: [기본(수정불가)▼] `검색(F8)` 금일 전일 금주(~오늘) 전주 금월(~오늘) 전월 종료일 다시작성 ∨

## 7. 전역 구조 (실물)

- 대메뉴: MyPage · Self-Customizing · 재고Ⅰ · 재고Ⅱ · 회계Ⅰ · 회계Ⅱ · 관리 · 세무 · 그룹웨어 · **데이터센터** (+우측 아이콘 ~20개, 최우측 프로필 원형)
- 재고Ⅰ 서브탭: 기초등록 영업관리 구매관리 생산/외주 기타이동 쇼핑몰관리 출력물
- 회계Ⅰ 서브탭: 기초등록 FastEntry 매출매입거래 전자(세금)계산서 계좌/카드 현금거래 비현금거래 어음거래 고정자산 회계거래관리 출력물
- 즐겨찾기 바(사용자 실사용 메뉴 = 구현 우선순위): 재고현황 · 거래처리스트 · 생산입고II-소모품목 선택 · 판매조회 · 거래명세서인쇄 · 거래처별채권 · 판매일괄회계반영 · (세금)계산서진행단계 · 입/출금계좌 조회 · 전표조회

## 8. 이번 라운드(R8) 우선순위

1. 디자인 토큰 정합 (styles.css **append-only**)
2. MyPage 위젯 구성·배치 실물화 (§2)
3. 재고현황 결과 화면 실물화 (§3)
4. 판매조회 그리드 실물화 (§4)
5. 거래처리스트 실물화 (§5)

다음 라운드(R9 후보): 생산입고II·거래처별채권·판매일괄회계반영·(세금)계산서진행단계·입/출금계좌조회·전표조회 (실물 캡처 후 진행).

## 9. 불변 규칙 (에이전트 공통)

- `client/src/styles.css`는 **append-only** (기존 줄 수정 금지, 끝에 덧붙여 override)
- 기존 메뉴 id 동결 (localStorage 즐겨찾기 보존) — 삭제·개명 금지, 추가는 허용
- 금액 INTEGER 원 / 수량 REAL kg, 그리드는 Tabulator(DataGrid 래퍼)
- 국세청/은행 연동은 '연동 예정' 비활성 stub 유지
- 커밋은 지휘관 전담 (에이전트는 커밋 금지), 모델 식별자를 코드·문서에 넣지 말 것
