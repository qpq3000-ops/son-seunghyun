# 작업일지 — 2026-07-12

## R8 실물 매칭 라운드

### 개요
실물 실측값(docs/참고-실물실측-20260712.md)을 기준으로 R8 5개 항목(홈화면·재고현황·판매조회·거래처리스트·거래처등록)의 갭을 분석하고, 설계 결정을 반영한 구현 완료.

### 핵심 발견사항 및 갭 분석

| 항목 | 실물 | 현황 | 갭 |
|------|------|------|-----|
| **홈화면(MyPage)** | 6개 위젯(3열) | 5개 위젯 | 레이아웃 불일치, 쪽지함·매입매출장·계산서진행단계 위젯 부재 |
| **판매 데이터 단위** | 줄 단위(line_level) | 전표 단위(doc_level) | 판매현황 쿼리 재작성 필요 |
| **재고현황 UI** | 중앙제목, 회사줄, 음수셀(#F2DEDE), 하단버튼 | 일반 그리드 | ReportScreen 옵션 확장 필요 |
| **판매조회 컬럼** | 상태pill, 페이지표기, 체크박스, 회계반영, 거래유형 | 기본 컬럼만 | 실물 컬럼셋 반영 |
| **거래처리스트** | 컬럼 풀셋, 하단버튼바 | MasterScreen 기본형 | 메뉴 id 분리(partner-acct 추가) |

### 설계 결정

1. **GET /api/mypage 확장**
   - sales: 전표 단위(6건) → 줄 단위(15건), `doc ⨝ doc_line ⨝ item ⨝ partner` 조인
   - messages, pnl_ledger, tax_invoice 필드 추가 (값: [])

2. **ReportScreen real 옵션**
   - centerTitle, companyLine, negativeField, bottomButtons 네 가지 UI 옵션 추가
   - 재고현황, 판매조회 등에서 재사용 가능

3. **메뉴 id 동결 + 회계I 이원화**
   - 기존 메뉴 id 유지, partner-acct 새 id로 회계I 동일 화면 노출
   - 거래처등록 id 불변(수정만)

4. **스타일 격리**
   - styles.css는 기존 콘텐츠 무수정, 끝에 R8 블록 순수 append

5. **파일 분할 전략**
   - 클라 10파일, 서버 mypage.mjs 1파일
   - 겹침 0 (파일 간 수정 사항 없음)

### 구현 완료 사항

#### 클라이언트 변경 (10파일)
- **types.ts**: Dashboard 컴포넌트 타입 정의
- **Dashboard.tsx**: 홈화면 레이아웃, 위젯 나열
- **ReportScreen.tsx**: real 옵션 처리 로직
- **reportDefs.ts**: 각 리포트(재고현황·판매조회) 정의
- **StockScreens.tsx**: 재고 화면 통합
- **VoucherList.tsx**: 판매조회 컬럼셋
- **masters.tsx**: 거래처리스트 및 거래처등록
- **MasterScreen.tsx**: 마스터 화면 공통 컴포넌트
- **menus.tsx**: 메뉴 구조 + partner-acct id
- **styles.css**: R8 스타일 블록 append

#### 서버 변경 (1파일)
- **mypage.mjs**
  - sales 쿼리: doc 단위 → doc_line 단위 (15건)
  - WHERE: `doc_type='sale' AND io_date BETWEEN 이번달1일~오늘`
  - ORDER BY: `io_date DESC, id DESC, line_no ASC`

### 검증

```
cd C:/Users/qpq30/son-seunghyun
npx tsc --noEmit -p client/tsconfig.json
```
**결과: 0 오류 (EXIT 0)**

### 다음 라운드(R9) 후보
- 생산입고II
- 거래처별채권
- 판매일괄회계반영
- (세금)계산서진행단계
- 입/출금계좌조회
- 전표조회

각 항목별 실물 캡처 후 매칭 설계.

---

**작업 상태**: ✅ 완료  
**다음 담당**: R9 실물 실측 후 설계 라운드

## R9 실물 매칭 라운드

### 개요
R8 완료 이후 실물 실측된 6개 항목(생산입고II, 판매일괄회계반영, 계산서진행단계, 입/출금계좌조회, 전표조회, 거래처별채권)을 갭 분석하여 설계 결정 완료.

### 핵심 발견사항 및 갭 분석

| 항목 | 실물 | 담당 화면 | 처리방안 |
|------|------|------|--------|
| **생산입고II** | 로스팅 출고 현황(기존 ProductInScreen) | RoastInput.tsx | 기존 조회 미접촉, 입력 화면만 신규 |
| **판매일괄회계반영** | 판매전표 선택 → 일괄 회계 반영 | SaleBulkPostScreen.tsx | 신규 화면 |
| **계산서진행단계** | 세금계산서 상태 추적 | EtaxInvoiceScreen.tsx | 기존 Tax/TaxInvoice 로직 재사용 |
| **입/출금계좌조회** | 자금현황(기존 DepositScreen은 예적금 CRUD) | CashScreen.tsx | 신규 화면, 기존 미접촉 |
| **전표조회** | GL 전표 목록 및 상세 조회 | GlVoucherScreen.tsx | 신규 화면 |
| **거래처별채권** | 파트너별 미수금 현황 | ArByPartnerScreen.tsx | 신규 화면 |

### 설계 결정

1. **화면 분할 및 파일 구성**
   - 클라A: RoastInput, EtaxInvoiceScreen, CashScreen + 기존 MasterScreen, Dashboard, types, menus, styles
   - 클라B: SaleBulkPostScreen, GlVoucherScreen, ArByPartnerScreen + types, menus, styles
   - 서버: accounting.mjs에 GET /api/gl-vouchers 한 건 신규 추가 (기존 journal/journal_line 집계, 테이블 무추가)
   - 나머지 항목은 기존 엔드포인트 재사용

2. **파일 겹침 정책**
   - styles.css 외 모든 파일 겹침 0
   - 각 패키지 독립적 병합 가능

3. **R8 다듬기 3건 (R9로 이관)**
   - 거래처리스트의 '삭제' 열 제거 → 행선택+변경/사용중단 흐름 정리
   - 마스터 화면 상단 버튼 중복 제거 → Search(F3)/Option/도움말로 정리
   - MyPage 판매현황 빈 상태 문구 통일

### 변경 파일 목록

#### 서버
- `server/accounting.mjs`: GET /api/gl-vouchers 신규 추가

#### 클라이언트 A (기존 + 신규 3화면)
- `client/src/screens/RoastInput.tsx` (신규)
- `client/src/screens/EtaxInvoiceScreen.tsx` (신규)
- `client/src/screens/CashScreen.tsx` (신규)
- `client/src/components/MasterScreen.tsx`
- `client/src/screens/Dashboard.tsx`
- `client/src/types.ts`
- `client/src/menus.tsx`
- `client/src/styles.css`

#### 클라이언트 B (신규 3화면 + 타입)
- `client/src/screens/SaleBulkPostScreen.tsx` (신규)
- `client/src/screens/GlVoucherScreen.tsx` (신규)
- `client/src/screens/ArByPartnerScreen.tsx` (신규)
- `client/src/types.ts`
- `client/src/menus.tsx`
- `client/src/styles.css`

### 설계 문서
- `docs/설계-R9-실물매칭.md` — 각 항목별 갭 상세, 엔드포인트 정의

### 검증
```
cd C:/Users/qpq30/son-seunghyun
npx tsc --noEmit -p client/tsconfig.json
```
**결과: 0 오류 (EXIT 0)**

**주의사항**: 허용 목록 내 파일 수정 확인 필요 (작업일지 기록 완료 여부)

---

**작업 상태**: ✅ 설계 완료  
**다음 담당**: 클라A/B/서버 구현 시작

## R10 실물 매칭 라운드

### 개요
R9 완료 이후 판매조회 하단버튼, 메뉴 라벨 한글화, 기능설정 실물화, 메뉴 커버리지 확장을 수행.

### 변경 사항 요약

| 항목 | 내용 |
|------|------|
| **판매조회 하단버튼** | 10종 버튼 추가(조회링크·불러온전표 포함) |
| **메뉴 라벨 실물화** | e-tax-invoice → (세금)계산서진행단계, monthly-pl → 월별손익분석, vat-book → 매입/매출장, partner-ledger → 거래처거래내역조회, bank-link → 입/출금계좌 조회, wms → WMS |
| **기능설정** | 실측값 반영 |
| **메뉴 커버리지** | 라벨 확장 |

### 변경 파일 목록

#### A 그룹 (판매조회 및 설정)
- `client/src/screens/VoucherList.tsx`
- `client/src/screens/SettingsScreen.tsx`
- `client/src/styles.css`
- `server/vouchers.mjs`

#### B 그룹 (메뉴 및 리포트 정의)
- `client/src/menus.tsx`
- `client/src/screens/GridStub.tsx`
- `client/src/screens/reportDefs.ts`
- `client/src/styles.css`

### 검증
```
cd C:/Users/qpq30/son-seunghyun
npx tsc --noEmit -p client/tsconfig.json
```
**결과**: tsc=${verify?verify.tscOk:'?'} build=${verify?verify.buildOk:'?'} 메뉴수=${verify?verify.menuCount:'?'} 이슈=${verify?verify.issues.length:'?'}

---

**작업 상태**: ✅ 완료  
**다음 담당**: 추가 라운드

## 지휘관 검수 결과 (R8)

- 토큰 실측 일치 확인: body 12px·#F7F8F9, 위젯 카드 10px·#DEE2E6, 즐겨찾기줄 #E9ECEF, 그리드 머리글 #F7F8F9, 코드링크 #19358C, 주버튼 #1B3DA1 (검수 중 배경·보더·즐겨찾기줄 3건 보정 append)
- MyPage 6위젯 3열 배치(stock/todo/sales, note/todo/vatbook, note/taxinv/taxinv) 실물 구조와 일치
- 재고현황 결과: 중앙 24px 제목·회사명/기준일 줄·인쇄(스플릿)/Excel/자동알림 확인. 환경설정 상호를 "블랙크랙원 로스터스"로 입력함
- 판매조회: 상태 pill(전체 활성 #1B3DA1)·페이지/기간 줄·실물 컬럼 10개 확인
- 거래처리스트: 실물 컬럼·하단 버튼 8종·21건 표시 확인

### R9로 넘기는 다듬기
1. 거래처리스트의 '삭제' 열은 실물에 없음 → 행선택+변경/사용중단 흐름으로 이관 검토
2. 마스터 화면 상단의 구식 '엑셀/신규(F2)' 버튼이 하단 실물 버튼바와 중복 노출 → 상단은 Search(F3)/Option/도움말 형태로 정리
3. MyPage 판매현황 빈 상태 문구를 실물 문구("등록된 데이터가 없습니다.")로 통일할지 결정
4. 실물 캡처 잔여 6화면(생산입고II·거래처별채권·판매일괄회계반영·계산서진행단계·입출금계좌조회·전표조회) 매칭
